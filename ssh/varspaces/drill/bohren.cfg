
    # do not edit the following section name:
[Version]
        
        # do not edit the following value:
        specversion = 4


[Variables]
        
        # persistent variables
        drill_axis = Z
        tool_dia = 8.0
        spindle_rpm = 500
        drill_axis_feedrate = 50.0
        drill_axis_safe_margin = 5.0
        drill_axis_drill_depth = 10.0
        peck_depth = 2.0
        use_pecking = True
        drill_points = True
        drill_circles = False

[UI]
        # Variables listed here are displayed in the UI and are editable
        # the string value is the descriptive text displayed in a Label
        # variables from the Variables section can be interpolated into
        # e.g. Label names
        
        
        #named frames:
        [[Tool Parameters]]
                tool_dia = Drill diameter [mm]:
        [[Depth Coordinates]]
                drill_axis_safe_margin = %(drill_axis)s safety margin [mm]:
                drill_axis_drill_depth = %(drill_axis)s drill depth [mm]:
        [[Rates]]
                spindle_rpm = spindle RPM [rev/min]:
                drill_axis_feedrate = %(drill_axis)s feed [mm/min]:
        [[Pecking]]
                use_pecking = Use G83 pecking
                peck_depth = peck depth
        [[Objects]]
                drill_points = Drill points
                drill_circles = Drill circles
    
    
