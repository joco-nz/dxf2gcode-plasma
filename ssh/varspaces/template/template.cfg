
        # do not edit the following section name:
[Version]
            
            # do not edit the following value:
            specversion = 12


[Variables]
            
            
            
            # persistent variables
            tool_dia = 63.0
            start_radius = 0.0
            axis3_safe_margin = 20.0
            axis3_slice_depth = 0.5
            axis3_mill_depth = 10.0
            f_g1_depth = 50.0
            f_g1_plane = 150.0
            # Tests
            booltrue = True
            boolfalse = False
            option = bar
            
            unframed_int = 50
            
            ax1_letter = X
            ax2_letter = Y
            ax3_letter = Z
            
            random_text = ver random


[Appearance]
            # Variables listed here are displayed in the UI and are editable
            # the string value is the descriptive text displayed in a Label
            # variables from the Variables section can be interpolated into
            # e.g. Label names
            
            [[FRAMED]]
                        random_text = Random Text
            
            [[UNFRAMED]]
                        unframed_int = Edit int value:
            
            
            #named frames:
            [[Tool Parameters]]
                        tool_dia = Tool diameter [mm]:
                        start_radius = Start radius (for tool comp.) [mm]:
            [[Depth Coordinates]]
                        axis3_safe_margin = %(ax3_letter)s safety margin [mm]:
                        axis3_slice_depth = %(ax3_letter)s infeed depth [mm]:
                        axis3_mill_depth = %(ax3_letter)s mill depth [mm]:
            [[Feed Rates]]
                        f_g1_depth = G1 feed %(ax3_letter)s-direction [mm/min]:
                        f_g1_plane = G1 feed %(ax1_letter)s%(ax2_letter)s-direction [mm/min]:
            
            [[Test]]
                        # pre-set boolean variables
                        booltrue = Checked
                        boolfalse = Unchecked
                        
                        # a OptionMenu - labeltext is first element, rest is choices:
                        option = Tick one of:, foo, bar, baz
        
        
